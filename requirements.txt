# Generacja datasetu
ollama

# Trening NER (mamba create -n nlp-ner python=3.12 && pip install -r requirements.txt)
torch
transformers
datasets
seqeval
accelerate
wandb
tqdm

# Appka anonimizacyjna (app/) — też używane przez HF Spaces
gradio
faker
